/**
 * TypeScript stub — Metro Bundler usa AdInterstitial.native.tsx (Android/iOS)
 * o AdInterstitial.web.tsx (Web) en tiempo de ejecución.
 * Este archivo solo existe para que tsc pueda resolver el módulo.
 */
export function useInterstitialAd(): { showAd: () => void } {
  return { showAd: () => {} };
}
