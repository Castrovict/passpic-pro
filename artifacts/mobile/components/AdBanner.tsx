/**
 * TypeScript stub — Metro Bundler usa AdBanner.native.tsx (Android/iOS)
 * o AdBanner.web.tsx (Web) en tiempo de ejecución.
 * Este archivo solo existe para que tsc pueda resolver el módulo.
 */
export function AdBanner(): null {
  return null;
}
